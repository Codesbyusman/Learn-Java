public class operators_v3 {
    public static void main(String[] args){
        int random = (int) (Math.random() * 50);

//        > < == != >= <=
        // logical operators
        // ! negqation
        // && and
        // & even if first is false will check the other (opposite to && working)
        // | or
        // ^ xor one tru one false (not bitwise)
        // ternary operator ? :

//        switch
        char character = 'B';

        switch(character)
        {
            case 'A':
                System.out.println("good job");
                break;
            default:
                System.out.println("lll");
        }

    }
}
